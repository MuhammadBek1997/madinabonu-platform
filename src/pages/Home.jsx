import { useGlobalContext } from '../Context'
import '../styles/Home.css'


const Home = () => {

let { setsToggle,sToggle } = useGlobalContext()

  return (
    <section>
     
    </section>
  )
}

export default Home